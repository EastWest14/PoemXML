package Cases;

public class CaseRunner {
	public static GroupRunResult execute(RegressionTestCase[] cases) {
		boolean passes = true;
		
		for (RegressionTestCase aCase: cases) {
			CaseRunResult result = new aCase.run();
		}
		
		return new GroupRunResult(false);
	}
}
